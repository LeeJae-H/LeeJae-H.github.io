---
title: "단위 테스트 예시"
tags:
    - java
date: "2025-01-05"
thumbnail: "/assets/img/thumbnail/sample.png"
---
> [[테스트 범위와 종류](https://leejae-h.github.io/TDD(Test-Driven%20Development)/테스트%20범위와%20종류.html)]를 먼저 읽고 오면 좋다.

# 단위 테스트 예시
## 테스트에 사용될 객체들
```java
@Entity
@NoArgsConstructor(access = AccessLevel.PROTECTED)
@Getter
public class User {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(name = "name")
    private String name;

    @Column(name = "age")
    private int age;

    @Builder
    public User(String name, int age) {
        this.name=name;
        this.age=age;
    }

    public void changeAge(int age) {
        this.age=age;
    }
}
```
```java
public interface UserRepository extends JpaRepository<User,Long> {
    Optional<User> findByName(String name);
}
```
```java
public interface UserService {
    Long createUser(String name, int age);
}
```
```java
@Service
@Transactional
@RequiredArgsConstructor
public class UserServiceImpl implements UserService {
    private final UserRepository userRepository;
    
    @Override
    public Long createUser(String name, int age) {
        userRepository.findByName(name)
            .ifPresent(user -> throw new ExpectedException(ErrorCode.ALREADY_EXISTED_USER););

        User user = User.builder()
                        .name(name)
                        .age(age)
                        .build();
        return userRepository.save(user).getId();
    }
}
```
```java
@Getter
@RequiredArgsConstructor
public class ExpectedException extends RuntimeException {
    private final ErrorCode errorCode;
}
```
```java
@Getter
@RequiredArgsConstructor
public enum ErrorCode {
    ALREADY_EXISTED_USER("ALREADY_EXISTED_USER", "이미 존재하는 유저입니다.");
    
    private final String errorCode;
    private final String errorMessage;
}
```


## 도메인 테스트
```java
public class UserTest {

    @Test
    @DisplayName("유저 생성")
    void createUser() {
        // given
        User user = User.builder().name("joyuri").age(20).build();
    
        // when & then
        assertThat(user.getName()).isEqualTo("joyuri");
        assertThat(user.getAge()).isEqualTo(20);
    }

    @Test
    @DisplayName("유저의 나이 변경")
    void changeUserAge() {
        // given
        User user = User.builder().name("joyuri").age(20).build();
    
        // when
        user.changeAge(25);
        
        // then
        assertThat(user.getAge()).isEqualTo(25);
    }
}
```

## 서비스 테스트
```java
@ExtendWith(MockitoExtension.class)
public class UserServiceTest {

    @Mock
    private UserRepository userRepository;

    @InjectMocks
    private UserService userService;

    @Test
    @DisplayName("유저 생성 성공")
    void createUser_Success() {
        // given
        User user = User.builder().name("joyuri").age(20).build();
        ReflectionTestUtils.setField(user, "id" ,3L);
    
        BDDMockito.given(memberRepository.save(user))
            .willReturn(user); // Stub

        // when
        Long userId = memberService.createUser("joyuri", 20);

        // then
        assertThat(userId).isEqualTo(3L);
    }

    @Test
    @DisplayName("유저 생성 시 이미 존재하는 유저이면 예외 발생")
    void createUser_AlreadyExistedUser_Then_Exception() {
        // given
        User existingUser = User.builder().name("joyuri").age(20).build();

        BDDMockito.given(userRepository.findByName("joyuri"))
            .willReturn(Optional.of(existingUser)); // Stub

        // when & then
        assertThatThrownBy(() -> userService.createUser("joyuri", 20))
            .isInstanceOf(ExpectedException.class)
            .satisfies(e -> {
                assertThat(e.getErrorCode()).isEqualTo(ErrorCode.ALREADY_EXISTED_USER);}
        );
    }
}
```